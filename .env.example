# The build takes no secrets: search is Pagefind, which is static files.
# These only affect local runs.

# Hugo's environment. `production` turns on minification and asset fingerprinting.
HUGO_ENVIRONMENT=development

# Where the Playwright suite points. Defaults to the local preview server.
BASE_URL=http://localhost:8080
